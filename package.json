{
  "name": "sandbagger-scoring",
  "version": "1.0.0",
  "description": "Golf tournament scoring and leaderboard application",
  "main": "index.html",
  "scripts": {
    "clean": "rm -rf www/* && mkdir -p www/api www/icons www/images www/js www/css",
    "copy:core": "cp index.html landing.html script.js help-content.js style.css manifest.json service-worker.js offline-sync.js favicon.png favicon.ico www/ 2>/dev/null || true",
    "copy:php": "find . -maxdepth 1 -name '*.php' -not -name 'admin.php' -exec cp {} www/ \\;",
    "copy:dirs": "cp -r api icons images www/ && cp -r pdfs www/ 2>/dev/null || true",
    "copy:admin": "cp admin.php tournament-assign.html www/ 2>/dev/null || true && cp -r js css www/",
    "build:user": "npm run clean && npm run copy:core && npm run copy:php && npm run copy:dirs",
    "build": "npm run build:user && npm run copy:admin",
    "sync:user": "npm run build:user && LANG=en_US.UTF-8 npx cap sync",
    "sync": "npm run build && LANG=en_US.UTF-8 npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "cap": "npx cap"
  },
  "keywords": [
    "golf",
    "scoring",
    "tournament",
    "leaderboard",
    "pwa",
    "capacitor",
    "mobile"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
